{% extends 'base.html.twig' %}

{% block body %}
    <h1>Gafa</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ gafa.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ gafa.model }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ gafa.image }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ gafa.price }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('gafas_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('gafas_edit', { 'id': gafa.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
